import { Text, View } from '@tarojs/components'
import styles from './index.module.css';
const Publish = () => {
  return (
    <View className={styles.item}>
      <Text className='text-green-300'>publish</Text>
    </View>
  )
}

export default Publish

